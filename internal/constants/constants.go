package constants

// DefaultPath for the server
const DefaultPath = "/"

// Version of the API
const Version = "v1"

const ProxyPath = DefaultPath + "proxy/"

const TestProxyPath = ProxyPath + "test/"
